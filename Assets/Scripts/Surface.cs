using UnityEngine;

public class Surface : MonoBehaviour
{
    public enum SurfaceType  {Sand, Road};

    [Header ("Surface")]
    public SurfaceType surfaceType;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
